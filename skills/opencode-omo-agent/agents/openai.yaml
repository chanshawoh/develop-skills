interface:
  display_name: "OpenCode OMO Agent"
  short_description: "Delegate work to OMO running inside OpenCode"
  default_prompt: "Use $opencode-omo-agent to have the current assistant orchestrate OMO Ultimate inside OpenCode; trigger /ulw-loop by placing it at the very beginning of the OpenCode message."

policy:
  allow_implicit_invocation: true
